// This file can be used for global Jest setup if needed.
// For example, setting up environment variables or custom matchers.

// In this project, no specific global setup is required beyond default Jest config.

// If using ES modules in Node.js, you might configure `ts-jest` to use ESM:
// module.exports = {
//   preset: 'ts-jest',
//   testEnvironment: 'node',
//   extensionsToTreatAsEsm: ['.ts'],
//   globals: {
//     'ts-jest': {
//       useESM: true,
//     },
//   },
// };
// However, the current `tsconfig.json` uses `commonjs`, so this is not needed.